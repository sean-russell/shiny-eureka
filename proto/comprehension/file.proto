syntax = "proto3";

package comprehension; 


message DataChunk {
  bytes data = 1;
}

service fileService {
  rpc DownloadProductImage(DataChunk) returns (Response);
}

message Response {
  bool received = 1;
}